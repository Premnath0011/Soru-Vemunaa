// Change only this file (or the REACT_APP_API_URL value in your .env) when the backend address changes.
// Falls back to localhost so the app works out of the box in development.
export const API_URL = process.env.REACT_APP_API_URL || 'http://10.169.133.83:8000/api';
