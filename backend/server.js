require('dotenv').config();const express=require('express'),cors=require('cors'),connectDB=require('./config/db');const app=express(),PORT=process.env.PORT||8000;app.use(cors());app.use(express.json({limit:'10mb'}));app.get('/api/health',(req,res)=>res.json({ok:true,app:'Soru Creator'}));app.use('/api/auth',require('./routes/authRoutes'));app.use('/api/projects',require('./routes/projectRoutes'));app.use('/api/transactions',require('./routes/transactionRoutes'));app.use('/api/ideas',require('./routes/ideaRoutes'));app.use('/api/categories',require('./routes/categoryRoutes'));app.use('/api/schedules',require('./routes/scheduleRoutes'));app.use('/api/reports',require('./routes/reportRoutes'));connectDB().then(()=>app.listen(PORT,'0.0.0.0',()=>console.log(`Soru API running on ${PORT}`))).catch(e=>{console.error('Database error:',e.message);process.exit(1)});
